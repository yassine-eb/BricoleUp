[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn bricoleup.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/v1/home/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
