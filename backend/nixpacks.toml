[phases.setup]
nixPkgs = ["python311Full", "libpq"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 -m gunicorn bricoleup.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
